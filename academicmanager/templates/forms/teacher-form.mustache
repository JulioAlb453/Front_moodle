<div class="card">
    <div class="card-header">
        <h3>Crear Docente</h3>
        <button class="btn btn-sm btn-secondary" onclick="hideForm()">Cancelar</button>
    </div>
    
    <form id="teacher-form" onsubmit="return handleTeacherSubmit(event)">
        <div class="row">
            <div class="col">
                <div class="form-group">
                    <label class="form-label">Nombre *</label>
                    <input type="text" class="form-control" name="first_name" required 
                           placeholder="Nombre">
                </div>
            </div>
            <div class="col">
                <div class="form-group">
                    <label class="form-label">Apellido *</label>
                    <input type="text" class="form-control" name="last_name" required 
                           placeholder="Apellido">
                </div>
            </div>
        </div>
        
        <div class="form-group">
            <label class="form-label">Email *</label>
            <input type="email" class="form-control" name="email" required 
                   placeholder="docente@universidad.edu">
        </div>
        
        <div class="form-group">
            <label class="form-label">Nombre de Usuario *</label>
            <input type="text" class="form-control" name="username" required 
                   placeholder="usuario.docente">
        </div>
        
        <div class="form-group">
            <label class="form-label">Contraseña Temporal *</label>
            <input type="password" class="form-control" name="password" required 
                   placeholder="Mínimo 8 caracteres" minlength="8">
            <small class="form-text text-muted">El docente deberá cambiar su contraseña al primer inicio de sesión</small>
        </div>
        
        <div class="form-group">
            <label class="form-label">Asignaturas a Impartir *</label>
            <select class="form-control" name="subjects[]" multiple required id="teacher-subjects-select" style="height: 150px;">
                {{#subjects}}
                <option value="{{id}}">{{nombre}} - C{{semester}} - {{program_name}}</option>
                {{/subjects}}
            </select>
            <small class="form-text text-muted">Mantén Ctrl (Cmd en Mac) para seleccionar múltiples asignaturas</small>
        </div>
        
        <div class="form-group">
            <label class="form-label">Departamento</label>
            <input type="text" class="form-control" name="department" 
                   placeholder="Ej: Departamento de Sistemas">
        </div>
        
        <div class="form-group">
            <label class="form-label">Título Profesional</label>
            <input type="text" class="form-control" name="professional_title" 
                   placeholder="Ej: Doctor en Ciencias de la Computación">
        </div>
        
        <button type="submit" class="btn btn-success">
            <span class="spinner-border spinner-border-sm d-none" id="teacher-spinner"></span>
            Crear Docente
        </button>
    </form>
</div>